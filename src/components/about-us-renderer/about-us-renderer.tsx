import * as React from 'react';
import * as styles from './about-us-renderer.css';

export const AboutUsRenderer = (props: {}) => {
  return (
    <div className={styles.about}>It's me, Sakshet! Your developer</div>
  );
}